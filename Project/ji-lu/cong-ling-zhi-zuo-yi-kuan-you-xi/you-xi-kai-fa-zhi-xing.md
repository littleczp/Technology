# 游戏开发-执行

{% code title="生成任务包Prompt" overflow="wrap" %}
```md
把以下任务转成一个任务包。接收方看不到本对话，可能也看不到仓库。

任务：
任务格式：任务/一句话目标/可写文件/状态 owner/必须在谁之后/建议执行方
任务内容：{{粘贴 开发策划 输出的那一条：任务 | 一句话目标 | 可写文件...}}


你的职责只是从仓库里摘出正确的代码片段并填进模板。
不要重新设计这个任务，不要扩大范围，不要质疑拆分方式。
如果你认为这个任务有问题，只提出来，不要自行修改。

## 生成前检查（不通过就停）
- 涉及文件是否真实存在？路径对不对？
- 执行方是网页 chat，但需要读 3 个以上文件 → 告诉我，别继续执行。
- 需要改 AGENTS.md 或术语表 → 停下，这不是任务包该干的事，告诉我应该回去找游戏策划。

## 任务包格式（严格照抄）

# TASK-{{id}}: {{标题}}

## 你要做什么（一句话）

## 你能改的文件
（明确列出。其他文件一律不许动，需要动请停下来告诉我。）

## 你需要知道的现有代码（已粘贴，不必询问）
（从仓库摘真实代码，不是描述。
 网页 chat 贴全；本地 agent 给路径 + 关键片段即可。）

## 约定
（只贴与本任务相关的那几条 AGENTS.md 规则，不要贴全文。）

## 完成的标准
（可机械验证：测试命令、具体用例数量、或"启动后屏幕上出现 X"。）

## 明确不需要你做的
（列出最容易被顺手扩展的相邻功能。）

## 遇到以下情况请停止并回复我
需要改约定 / 需要新事件 / 发现现有代码有 bug

## 硬约束
- 任务包必须能塞进一次会话。塞不进就回去找策划拆。
- 一个任务包只碰一个状态 owner。
```
{% endcode %}
