# 游戏开发

{% code title="生成任务包Prompt" overflow="wrap" %}
```md
把以下需求转成一个任务包。接收方看不到本对话，可能也看不到仓库。

需求：{{一句话}}
执行方：{{网页chat | 本地agent | 我自己}}

## 生成前先判断（不通过就别生成）
- 需要读 3 个以上文件才能做对 → 不适合网页 chat，告诉我换执行方。
- 需要改 AGENTS.md 或术语表 → 停下，先跟我讨论。
- 撞上 ADR 里的非目标清单 → 停下，告诉我撞了哪条。

## 任务包格式（严格照抄）

# TASK-{{id}}: {{标题}}

## 你要做什么（一句话）

## 你能改的文件
（明确列出。其他文件一律不许动，需要动请停下来告诉我。）

## 你需要知道的现有代码（已粘贴，不必询问）
（贴真实代码，不是描述。网页 chat 贴全，本地 agent 给路径即可。）

## 约定
（只贴与本任务相关的那几条，不要贴整个 AGENTS.md。）

## 完成的标准
（可机械验证：测试命令、具体测试用例数量。）

## 明确不需要你做的
（列出最容易被顺手扩展的相邻功能。）

## 遇到以下情况请停止并回复我
需要改约定 / 需要新事件 / 发现现有代码有 bug

## 硬约束
- 单个任务包必须能塞进一次会话。塞不进就拆分。
- 一个任务包只碰一个状态 owner。跨 owner 的拆成两个 + 一个集成任务。
- 你需要明确告诉我任务包的顺序，以及能否并行处理。
- 同时派发的任务包不得共享可写文件。有冲突就串行。
```
{% endcode %}
