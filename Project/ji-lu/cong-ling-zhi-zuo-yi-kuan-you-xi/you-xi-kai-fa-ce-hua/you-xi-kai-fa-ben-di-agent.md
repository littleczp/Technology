# 游戏开发-本地agent

{% code title="生成任务包Prompt" overflow="wrap" %}
```md
把以下任务转成一个任务包。

任务：
任务格式：任务/一句话目标/可写文件/状态 owner/必须在谁之后/建议执行方
任务内容：{{粘贴 开发策划 输出的那一条：任务 | 一句话目标 | 可写文件...}}

你的职责只是填写模板，不要重新设计这个任务，不要扩大范围，不要质疑拆分方式。
如果你认为这个任务有问题，只提出来，不要自行修改。

## 生成前检查（不通过就停）：涉及文件是否真实存在？路径对不对？

## 任务包格式（严格照抄）

# TASK-{{id}}: {{标题}}

## 你要做什么（一句话）

## 约定
（只贴与本任务相关的那几条 AGENTS.md 规则，不要贴全文。）

## 完成的标准
（可机械验证：测试命令、具体用例数量、或"启动后屏幕上出现 X"。）

## 明确不需要你做的
（列出最容易被顺手扩展的相邻功能。）

## 遇到以下情况请停止并回复我
需要改约定 / 需要新事件 / 发现现有代码有 bug

## 硬约束
- 任务包必须能塞进一次会话。塞不进就回去找策划拆。
- 一个任务包只碰一个状态 owner。
```
{% endcode %}

***

{% code title="本地 Agent" overflow="wrap" %}
```md
完成图中的所有任务，逐个执行。

你的职责只是完成任务，不要重新设计这个任务，不要扩大范围，不要质疑拆分方式。
如果你认为这个任务有问题，只提出来，不要自行修改。

## 执行前检查（不通过就停）：
- 涉及文件是否真实存在？路径对不对？
- 已阅读 AGENTS.md 规则
- 明确哪些是不需要你做的（特别是顺手做的）

## 遇到以下情况请停止并回复我
需要改约定 / 需要新事件 / 发现现有代码有 bug

没有疑问后，现在开始逐个执行任务。
```
{% endcode %}
